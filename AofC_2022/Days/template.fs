module Template

open Tools

let part1 input =
    let rows = Parsing.parseRows input (fun f -> f)
    let result = 0
    result
    
let part2 input =
    let rows = Parsing.parseRows input (fun f -> f)
    let result = 0
    result
